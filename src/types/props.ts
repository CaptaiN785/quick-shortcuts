export type TitleStyle = {
    fontSize: string
    color: string
}

export type TitleProps = {
    title: string
    titleStyle: TitleStyle
}

export type SearchStyle = {
    fontSize: string
    color: string
    background: string
}

export type SearchAlgoType = 'normal' | 'word-break'
